export async function handleEnvoiInbound(event: any) {
  // Example event: {from, to, message}
  console.log('envoi inbound', event);
  // Map inbound message to market actions or notifications
  // e.g., SMS to create alerts or community messages
}
