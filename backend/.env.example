# Backend server
PORT=5000
AI_SERVICE_URL=http://ai:5000/classify

# Postgres
PG_HOST=db
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=changeme123   # <-- change this before use
PG_DB=prediction_market

# Hedera & Sponsors (placeholders)
HEDERA_NETWORK=testnet
HEDERA_OPERATOR_ID=0.0.xxxx
HEDERA_OPERATOR_KEY=__PLACEHOLDER_DO_NOT_COMMIT__

ENVOI_API_KEY=__PLACEHOLDER__
ENVOI_WEBHOOK_SECRET=__PLACEHOLDER__

# Ethereum (for oracle bridge) — keep placeholders for now
RPC_URL=https://rpc.goerli.example
PRIVATE_KEY=__PLACEHOLDER__
MARKET_CONTRACT_ADDRESS=__PLACEHOLDER__

# Notes:
# - Replace placeholders with real values in your local .env, but never commit secrets.
# - For production, inject secrets using your secret manager (e.g., GitHub Actions secrets, Azure Key Vault).
